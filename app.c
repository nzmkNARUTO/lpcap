#include <stdio.h>
#include <ncurses.h>

#include "app.h"
#include "capture.h"

int main(){
    //initscr();
    //box(stdscr, ACS_VLINE, ACS_HLINE);
    getDevices();
    //mvprintw(LINES/2, COLS/2, "hello");
    //refresh();
    //getch();
    //endwin();
    return 0;
}